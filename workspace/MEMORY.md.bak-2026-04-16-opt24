# MEMORY.md

## Long-term notes

- 用户偏好中文交流，喜欢直接、少废话、先讲结论的回答风格。
- Leo 偏好“直接执行 + 分阶段反馈”，不喜欢长解释后没有产出。
- 复盘类任务不能只基于当前窗口下结论，必须先补足相关会话、transcript、工具结果和关键文件。
- `MEMORY.md` 只承接周复盘提炼出的长期规则 / 长期结论，不承接周报式细节与过程摘要。
- 对所有定时流程，是否真正“跑通”不能只看文件产出，还必须确认用户收到了清晰完成通知。
- agent 路由先判 3 件事：任务目标、输入状态、目标输出；先判角色，再选 agent。
- 输入状态统一分为：`原始材料`、`结构化中间层`、`草稿`、`已确认成品/执行指令`。
- 学习 / 研究 / 取证型 agent 只处理 `原始材料` 与证据；负责学习、提炼规则、识别边界，不直接出最终成稿。
- 整理 / 规划 / 抽象型 agent 负责把学习结果收敛成 `结构化中间层`；不直接承担最终写作或执行。
- 写作 / 生成型 agent 只接 `结构化中间层` 或已确认 brief；不得直接吃 `原始材料`。
- 审核 / 评估型 agent 只审 `草稿` 或既有结果；不代替学习或写作，除非其角色声明里明确同时具备对应能力。
- 执行 / 发布型 agent 只接 `已确认成品/执行指令` 或审核后的结果；不得从原始材料直接推断外发动作。
- 新增 agent 时，先登记 5 项：`role`、`accepts`、`produces`、`may_follow`、`forbidden`；未登记前，不接入正式链路。
- 当前已注册默认映射：`learning_agent` = 学习/提炼/边界识别；`organizing_agent` = 结构化中间层/规划/整理；`writing_agent` = 成稿/重写/页面文案。
- 对“原始材料 → 可发布文案”类任务，当前默认示例链路仍是：`learning_agent` → `organizing_agent` → `writing_agent`；这是当前注册默认链路，不是所有任务的唯一固定链路。
- 分配任务时按职责边界判断，不按“谁也能做”判断；不能把能做当成该做。
- 一旦发现任务已分错 agent，先停止并回退到正确角色，再继续执行。
