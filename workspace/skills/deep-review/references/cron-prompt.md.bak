# Deep Review 定时执行提示词

以下内容用于定时执行 `deep-review`。
它们不是说明文档，而是可直接用于调度任务的执行提示词。

---

## 每天 22:00：日复盘提示词

执行 `deep-review`，完成今天的**日复盘**。

### 硬性要求
1. 复盘范围必须覆盖今天完整自然日：`00:00-23:59`
2. 不能只看最后一段内容，不能只看当前窗口
3. 必须先枚举今天全部候选 session / transcript，生成当天 session 索引，再从中筛出全部相关会话，最后按任务链和时间线重构当天过程
4. 群聊是主要输入源之一
5. **所有用户消息都算输入**，不能因为后续输出要压缩，就在输入阶段随意裁剪
6. 助手消息只纳入以下类型：
   - 承诺
   - 结论
   - 错误
   - 纠偏
7. 必须纳入工具执行结果
8. 必须纳入与结果直接相关、且实际写入/修改过的关键文件
9. 最终输出可以压缩，但不能漏掉：
   - 关键转折点
   - 关键纠偏点
   - 关键确认点
   - 关键结果变化点
10. 不得把日复盘内容写入 `MEMORY.md`

### “相关会话”判定规则
以下内容应视为“相关会话”：
- 当天属于同一任务链的群聊
- 当天直接影响结果的 direct 会话
- 当天与任务推进直接相关的子会话 / 子 session
- 出现目标变化、纠偏、确认、规则收口、关键结论的会话
- 有实际工具执行、文件改动、结果承接的会话

以下内容通常不纳入：
- 与当天主要任务无关的闲聊
- 没有影响结果的旁支对话
- 与任务链无连接的噪音会话

### “关键文件”判定规则
仅纳入这些文件：
- 规则文件
- skill 文件
- prompt 文件
- 配置文件
- 直接影响任务结果的输出文件

通常不纳入：
- 纯临时文件
- 与结论无关的中间文件
- 没有影响结果的无关改动文件

### 日复盘输出结构
1. 今日关键任务
2. 今日关键对话
3. 今日关键事实 / 关键结果
4. 今日关键偏差
5. 偏差开始节点
6. 根因分析
7. 复盘范围

### 日复盘写作要求
- 即使当天内容较少，也必须保留完整结构
- 可以简写，但不能删模块
- 当前会话只是触发与通知落点，不代表复盘范围仅限当前会话
- `复盘范围` 必须明确说明：
  - 看了哪些会话
  - 是否跨会话
  - 是否包含群聊
  - 是否包含子会话
  - 是否包含工具结果
  - 是否包含关键文件

### Session 索引要求
- 写正式日复盘前，必须先读取并遵循：
  - `memory/review/session-history-index.json` （获取当天所需所有会话列表唯一权威源）
  - `references/session-index-template.md`
  - `references/discovery-prompt.md`
- 必须先写出当天 session 索引，记录：
  - 全部候选 session / transcript
  - 判定为相关的项
  - 判定为不相关的项
  - 最终纳入复盘的证据集合
  - 覆盖风险与缺口

### 输出路径
将最终结果写入：
- `memory/review/index/YYYY-MM-DD-session-index.md`
- `memory/daily-review/YYYY-MM-DD.md`

如果当天文件已存在，直接覆盖，保留最新版。

---

## 每周日 22:00：周复盘提示词

执行 `deep-review`，完成本周的**周复盘**。

### 硬性要求
1. 复盘范围必须覆盖本周完整自然周
2. 不能把单一会话当作全部证据来源
3. 必须先枚举本周全部候选 session / transcript，生成本周 session 索引，再从中筛出全部相关会话，最后按重复任务链和时间线重构本周过程
4. 群聊是主要输入源之一
5. **所有用户消息都算输入**，不能在输入阶段随意裁剪
6. 助手消息只纳入以下类型：
   - 承诺
   - 结论
   - 错误
   - 纠偏
7. 必须纳入工具执行结果
8. 必须纳入与结果直接相关、且实际写入/修改过的关键文件
9. 必须识别：
   - 重复任务模式
   - 重复对话模式
   - 重复偏差
   - 根因归类
10. 最终输出可以压缩，但不能漏掉：
   - 本周关键转折
   - 重复出现的问题
   - 被多次强调的要求
   - 规则收口点
11. 必须评估每条候选长期规则 / 长期结论是否达到沉淀阈值
12. **只有达到阈值时，才允许更新 `MEMORY.md`**
13. 如果没有达到阈值，必须明确写：
   - 本周无新增长期规则
   - 和/或 本周无新增长期结论
14. 周复盘完成后，**无论是否更新 `MEMORY.md`，都必须通知用户**

### “相关会话”判定规则
以下内容应视为“相关会话”：
- 本周属于同一任务链的群聊
- 本周直接影响结果的 direct 会话
- 本周与任务推进直接相关的子会话 / 子 session
- 出现目标变化、纠偏、确认、规则收口、关键结论的会话
- 有实际工具执行、文件改动、结果承接的会话

以下内容通常不纳入：
- 与本周主要任务无关的闲聊
- 没有影响结果的旁支对话
- 与任务链无连接的噪音会话

### “关键文件”判定规则
仅纳入这些文件：
- 规则文件
- skill 文件
- prompt 文件
- 配置文件
- 直接影响任务结果的输出文件

通常不纳入：
- 纯临时文件
- 与结论无关的中间文件
- 没有影响结果的无关改动文件

### 长期沉淀阈值（正式版）
一条候选长期规则 / 长期结论，只有在满足以下条件时，才允许写入 `MEMORY.md`。

#### 基础前提
必须同时满足：
1. **不是一次性细节**
   - 不是某次偶发情况
   - 不是某周特殊背景下才成立的局部信息
2. **能脱离原上下文独立成立**
   - 未来再次看到这条内容时，不依赖整篇周复盘也能理解并使用

#### 核心判断（至少满足两项）
1. **重复出现**
   - 同一周内多次出现
   - 或跨天 / 跨任务反复出现
   - 或本周再次出现过去复盘里提到过的同类规律 / 同类问题
2. **影响结果**
   - 明显影响结果质量、效率、返工、判断或用户体验
3. **未来还会遇到**
   - 属于高频场景、固定流程或系统性问题，未来仍有复用价值

### 未达阈值时的处理
如果候选内容未达到阈值：
- 不更新 `MEMORY.md`
- 保留在周复盘详细文件中
- 可以写成“候选长期规则 / 候选长期结论”继续观察
- 但不能强行写入 `MEMORY.md`

### 周复盘输出结构
1. 本周主要工作
2. 本周反复出现 / 被强调的事
3. 本周关键事实
4. 本周重复偏差
5. 本周根因归类
6. 本周产出
7. 下周动作
8. 长期规则沉淀
9. 复盘范围

### 周复盘写作要求
- 不能简单拼接 7 天日复盘
- 必须抽取重复模式和重复根因
- 当前会话只是触发与通知落点，不代表复盘范围仅限当前会话
- `复盘范围` 必须明确说明：
  - 看了哪些会话
  - 是否跨会话
  - 是否包含群聊
  - 是否包含子会话
  - 是否包含工具结果
  - 是否包含关键文件
- `长期规则沉淀` 必须明确区分：
  - 已沉淀进入 `MEMORY.md` 的内容
  - 仍处于候选观察阶段的内容

### Session 索引要求
- 写正式周复盘前，必须先读取并遵循：
  - `memory/review/session-history-index.json` （获取本周所需所有会话列表唯一权威源）
  - `references/session-index-template.md`
- 必须先写出本周 session 索引，记录：
  - 全部候选 session / transcript
  - 判定为相关的项
  - 判定为不相关的项
  - 最终纳入复盘的证据集合
  - 覆盖风险与缺口

### 输出路径
将最终结果写入：
- `memory/review/index/YYYY-MM-DD-weekly-session-index.md`
- `memory/review/weekly/YYYY-MM-DD-weekly.md`

如果当周文件已存在，直接覆盖，保留最新版。

### 周复盘完成后的通知要求
周复盘完成后，必须通知用户。

#### 通知落点
- 默认通知到**当前主会话 / 当前用户可见会话**
- 不要默默完成不汇报

#### 通知内容必须包含
1. 周复盘已完成
2. `MEMORY.md` 是否更新
3. 如果已更新：
   - 概述新增的长期规则 / 长期结论
4. 如果未更新：
   - 明确说明本周没有新增长期规则 / 长期结论
5. 如果存在候选项：
   - 可简要说明仍在观察中的候选长期规则 / 候选长期结论

#### 通知风格
- 简洁
- 结构清楚
- 先给结论，再给必要说明
- 不重复整篇周复盘正文

#### 通知模板
发送通知前，读取并遵循：
- `references/weekly-notification-template.md`
