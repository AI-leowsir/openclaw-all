# 来源摘要

## 读取结果
- `task.json`：已读取，任务类型为 `learn_and_build_playbook`，页面类型为 `product-page`，语言为 `zh-CN`。
- `inputs/source-list.yaml`：存在，但内容仍为模板示例，包含 `example.com` 与占位说明，不可作为真实学习来源。
- `inputs/raw/README.md`：存在，仅为使用说明。
- `inputs/attachments/`：目录存在，但未发现附件。

## 结论
当前输入不足，无法开展有效学习、标准化提取与观察分析。

## 建议补充
1. 至少提供一个真实来源（域名、页面 URL、样文、文档、图片或视频）。
2. 在 `source-list.yaml` 中把模板项替换为真实来源，并标注 `must` / `reference`。
3. 如有本地材料，请放入 `inputs/raw/` 或 `inputs/attachments/`。
