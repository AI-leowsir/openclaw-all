# 阻塞说明

当前任务因输入不足而阻塞。

## 阻塞原因
- `inputs/source-list.yaml` 仍是模板内容，不含真实待学习来源。
- `inputs/raw/` 仅有 `README.md` 说明文件，没有原始正文或导出内容。
- `inputs/attachments/` 为空，没有文档、图片、视频等附件。

## 需要补充
- 真实域名或页面 URL
- 样文/文档/图片/视频中的至少一种
- 来源备注：为什么必须学、想抽取什么、目标页面或目标受众是什么

## 当前处理策略
已按 waiting_input 状态落盘，并生成最小占位输出，供上游判断是否补料重试。
